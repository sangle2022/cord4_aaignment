# Optional. Leave unset for local dev — requests use `/api` and Vite proxies to the backend
# (avoids clashing with SPA routes like /payouts on refresh).
# For production or calling API directly, set the full origin:
# VITE_API_URL=https://cord4-aaignment.onrender.com/
